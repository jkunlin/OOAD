merge branch_name命令:合并分支

用户输入merge <branch_name>命令
	if (当前暂存区是干净的){
		将当前分支的当前版本，与<branch_name>分支的当前版本进行合并
		生成新的版本
		将新版本的父版本设为当前分支的当前版本，以及<branch_name>分支的当前版本
		将当前分支的当前版本设为该新版本
	}
	else {
		提示用户先处理暂存区内容，或执行强制merge命令，抛弃暂存区内容
		结束用况
	}