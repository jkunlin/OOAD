commit [description]

if(输入强制commit命令) {
		if(用户未输入description) {
			提示用户输入description;
输入description
		}
		给暂存区生成快照作为一次新版本；
		记录并输出当前commit信息，包括commit_id, author, data, description
		设置当前分支的当前提交为该新版本，即，将HEAD设为该新版本 
}
if(输入commit命令) {
		if (存在新增、已修改，但尚未add的文件 || 存在已删除，但未rm的文件){
			提示用户add或rm后，再执行commit命令;
			结束用况;
		}
		if(用户未输入description) {
			提示用户输入description;
输入description
		}
		给暂存区生成快照作为一次新版本；
		记录并输出当前commit信息，包括commit_id, author, data, description 
		将该版本设置为当前分支的最新版本
}
